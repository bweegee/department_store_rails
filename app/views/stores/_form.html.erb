<h1><%= @store.id ? 'Edit Department Store' : 'Create Department Store' %></h1>
<hr />
<%= form_for @store do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>
